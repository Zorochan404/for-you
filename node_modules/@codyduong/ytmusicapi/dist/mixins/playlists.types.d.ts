import { parsePlaylistItemsReturn } from '../parsers/playlists.types';
import { thumbnails } from '../types';
export declare type PrivacyStatus = 'PUBLIC' | 'PRIVATE' | 'UNLISTED';
export declare type AddPlaylistItemsOptions = {
    videoIds: string[];
    sourcePlaylist?: string;
    duplicates?: boolean;
} | {
    videoIds?: string[];
    sourcePlaylist: string;
    duplicates?: boolean;
};
export declare type getPlaylistReturn = {
    id: string;
    privacy: PrivacyStatus;
    title: string;
    thumbnails: thumbnails;
    description?: string | null;
    author?: {
        name: string;
        id: string | null;
    };
    year?: string;
    duration?: string;
    trackCount: number;
    suggestions_token: any | null;
    tracks: parsePlaylistItemsReturn;
    duration_seconds: number;
};
export declare type addPlaylistItemsReturn = {
    status: string;
    playlistEditResults: any[];
} | Record<string, any>;
