"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.nav = void 0;
function nav(root, path, def) {
    try {
        if (root) {
            for (const k of path) {
                const accessor = typeof k == 'number' ? (k < 0 ? root.length + k : k) : k;
                root = root[accessor];
            }
            if (root === undefined || root === null) {
                return def;
            }
            else {
                return root;
            }
        }
    }
    catch (e) {
        if (typeof def !== 'undefined' &&
            (e instanceof ReferenceError || e instanceof TypeError)) {
            return def;
        }
        else {
            throw e;
        }
    }
    return def;
}
exports.nav = nav;
