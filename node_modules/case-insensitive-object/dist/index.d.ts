import { AllCase } from './types';
export default function CaseInsensitiveObjectFactory<T extends Record<any, any>>(value: T): CaseInsensitiveObject<T> & AllCase<T>;
export declare class CaseInsensitiveObject<T extends Record<any, any>> {
    #private;
    constructor(value: T);
    static fromEntries<T = any>(entries: Iterable<readonly [PropertyKey, T]>): {
        [k: string]: T;
    };
    static fromEntries(entries: Iterable<readonly any[]>): any;
}
